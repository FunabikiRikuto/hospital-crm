{
  "name": "hospital-crm",
  "version": "1.0.0",
  "description": "Hospital CRM Monorepo",
  "scripts": {
    "vercel-build": "cd frontend && npm install && npm run build && cd .. && cp -r frontend/.next .next && cp -r frontend/public public"
  },
  "private": true
}